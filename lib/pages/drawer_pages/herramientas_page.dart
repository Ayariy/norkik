import 'package:flutter/material.dart';

class HerramientasPage extends StatelessWidget {
  const HerramientasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Herramientas page'),
      ),
    );
  }
}
