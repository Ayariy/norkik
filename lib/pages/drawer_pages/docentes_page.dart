import 'package:flutter/material.dart';

class DocentesPage extends StatelessWidget {
  const DocentesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Doncentes page'),
      ),
    );
  }
}
