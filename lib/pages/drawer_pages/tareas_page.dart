import 'package:flutter/material.dart';

class TareasPage extends StatelessWidget {
  const TareasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tareas page'),
      ),
    );
  }
}
